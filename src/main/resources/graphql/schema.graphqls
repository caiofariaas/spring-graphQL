type Query{
    books: [Book]               # Retorna uma lista de livros
    bookById(id: Int): Book     # Livros por Id
}

type Book {
    id: ID
    name: String
    pageCount: Int
    author: Author
}

type Author{
    id: ID
    name: String
}